# Implementation Tasks And Roles

## 1. 目的
- MVP実装を、役割分担と完了条件を明確にして進める

## 2. 役割定義
### 2.1 設計者
- 仕様変更の判断責任を持つ
- `docs/02`〜`docs/10` の整合性を担保する
- 実装開始前に「仕様確定」を宣言する

### 2.2 実装者
- `front/` のみを変更する
- 実装は `docs/03` と各詳細仕様（`docs/05`〜`docs/10`）に従う
- E2Eが通る状態でPRを作成する

### 2.3 レビュー者
- 仕様適合、挙動、リスク、テスト網羅を確認する
- 指摘は再現手順付きで記載する
- 受け入れ基準は `docs/04.e2e-cases.md` を正とする

## 3. 進行フロー
1. 設計者が対象タスクを選定し、仕様確定を宣言
2. 実装者が機能実装とE2E追加を行う
3. レビュー者が仕様適合レビューとE2E結果を確認
4. 指摘修正後、再レビューして完了

## 4. タスク一覧（MVP）
| ID | タスク | 主担当 | 依存 | 完了条件（DoD） |
| --- | --- | --- | --- | --- |
| T1 | `front/` のNext.js+TS+Tailwind+Playwright初期化 | 実装者 | なし | `pnpm dev` と `pnpm test:e2e` が実行可能 |
| T2 | 型定義とRepositoryインターフェース作成 | 実装者 | T1 | `Book/ProgressLog/Reflection` と `listProgressLogs` を含むI/Fが実装される |
| T3 | LocalStorageRepository実装（復旧処理含む） | 実装者 | T2 | `book-reading-record.v1` 保存/読込/破損復旧が動作 |
| T4 | ダッシュボード画面実装（一覧・検索・週次） | 実装者 | T3 | 4ステータス一覧、感想未記入、検索、週次表示ができる |
| T5 | 書籍登録画面実装 | 実装者 | T3 | 必須項目バリデーション付きで登録できる |
| T6 | 書籍詳細・進捗記録画面実装 | 実装者 | T3 | ページ更新、メモ、履歴表示、状態更新ができる |
| T7 | 完読時感想と再読導線実装 | 実装者 | T6 | 完読時感想保存、再読時ページ0リセット、感想再編集が動作 |
| T8 | 入力検証とエラーメッセージ整備 | 実装者 | T4,T5,T6,T7 | `docs/07` の制約どおりに検証される |
| T9 | Playwright E2E作成（Case 1-17） | 実装者 | T4,T5,T6,T7,T8 | `docs/04` の全ケースが自動実行される |
| T10 | 仕様適合レビュー | レビュー者 | T9 | 重大指摘が0件、または修正完了 |
| T11 | 最終設計確認 | 設計者 | T10 | 実装結果と仕様の差分が解消されている |

## 5. レビューチェックリスト
- `base/` に変更がない
- `front/` のみ変更されている
- `docs/04` のCase 1-17が再現またはE2Eで通過
- データ破損時にクラッシュしない
- 再読時の `currentPage=0` / `completedAt` 解除 / `reflection` 保持が満たされる
- 再完読時に `reflection` が再編集（上書き保存）できる
- 並び順と週次集計が `docs/06` と一致する
- ユーザー/プロフィールのデータモデルがMVPに混入していない

## 6. 進捗管理ルール
- タスク状態は `todo` / `doing` / `review` / `done` の4状態
- 1PRで扱うタスクは最大2件まで
- 仕様変更が必要になった場合は、実装前に設計者へ差分提案する

## 7. 担当者記入欄
| 役割 | 氏名/ID |
| --- | --- |
| 設計者 | TBD |
| 実装者 | TBD |
| レビュー者 | TBD |
